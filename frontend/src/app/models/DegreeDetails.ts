import { CheckboxRequiredValidator } from "@angular/forms";

export interface degreeDetails {
    degreeName1: CheckboxRequiredValidator;
    degreeName2: CheckboxRequiredValidator;
    degreeName3: CheckboxRequiredValidator;
}
